# =====================================================
# Aerowise Frontend — Variables de Entorno
# =====================================================
# Copiar como .env para producción.
# En desarrollo local no hace falta (Vite proxy a localhost:8000).

# URL base del backend (dejar vacío en desarrollo, poner URL completa en producción)
# Ejemplo: https://aerowise-api.onrender.com
VITE_API_URL=
